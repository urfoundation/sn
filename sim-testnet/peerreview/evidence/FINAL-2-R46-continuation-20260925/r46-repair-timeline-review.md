# R46 signed repair predecessor chronology qualification

Commit `b9c725bac4543bba7dab55e657410c6d252100cb`, based on `6adc7be2`, fixes the historical chronology builder's remaining ordinary-upgrade assumption. A carried corrective activation previously compared its execution state with the source plan's older upgrade baseline. It now compares both implementation address and runtime hash with the exact retained upgrade in the already-authenticated signed corrective request. Ordinary repeated upgrades retain the original baseline check.

The synthetic regression deterministically failed before the fix in two ways: the valid constructor → ordinary upgrade → signed correction was rejected with “historical coordinator upgrade repair.coordinator-rounding.activate baseline does not chain from the preceding transition”; removing the ordinary predecessor was wrongly accepted. `prefix-red.log` and `.exit` retain both failures (exit 1, 2.318 seconds). The fixed three-test selection passed in 2.767 seconds. Its controls require a retained predecessor and reject a different retained runtime; the positive case also rebuilds and verifies the timeline artifact.

The combined historical selection passed normally in 37.614 seconds and with race detection in 328.780 seconds, both exit 0. `qualification.json` records exact argument arrays, source hashes, module pins, and log hashes. Both commands used Go 1.26.6, `GOMAXPROCS=4`, `-p 2`, private TMPDIR, and the existing pinned `r45-final.mod`. The first unpinned build hit the existing local server/operator-proxy geolocate dependency mismatch before test execution; it is recorded separately in `local-dependency-setup.log` and is not the causal red result.

`retained-field-comparison.json` records the bounded local R46 field comparison and exact source file hashes. The current carry's request and result equal the original signed artifacts. The ordinary activation at block 7,973,227 names exactly the retained upgrade of the corrective activation at block 7,986,580. The repair source's v4 baseline has a different implementation and runtime; applying that old baseline to the correction therefore rejects this retained history. No raw incident values were copied into tests.

The source delta is confined to the corrective predecessor branch. A sibling search found no other historical reader comparing the source upgrade baseline to transition state. Existing carried-action, signed-result, chronology, capture and artifact-census controls passed in the combined runs. This does not establish full semantic acceptance: the sealed R46 owner result is unchanged, and the active diagnostic was not altered. No RPC was issued for this review.

A scratch replay helper and a sealed-copy staging script are prepared outside the source repository. They require a completed diagnostic with passing canonical-chain, closed-foundation, and original-result checks; consume only copied hash-pinned bundles; validate the full retained plan/journal/request lineage; and invoke the production timeline builder plus independent artifact reconstruction. They have no network or validator re-capture path. The retained replay will be recorded separately after V3 seals its required bundle inputs.
