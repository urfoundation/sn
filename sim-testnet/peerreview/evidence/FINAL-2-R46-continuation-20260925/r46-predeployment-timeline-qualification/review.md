# R46 predeployment coordinator chronology

The timeline previously required a nonzero coordinator proxy on every retained plan. The earliest four approved plans predate the deployment identity envelope, so their decoded deployment is empty even though they approve future coordinator work. Treating these plans as unapproved incorrectly refused the complete historical lineage before RPC.

The exact retained plans are schema v2 `3e185d04...`, schema v1 `6c38dec8...`, schema v2 `d5f67a4c...`, and schema v3 `f8c4aae7...`. All four retain future symbolic coordinator actions. They have 0, 16, 92, and 12 finalized rows respectively, as recorded in `retained-zero-proxy-summary.json`. Only the v3 plan has a finalized Evm preparation: `evm.coordinator-implementation`, journal sequence 647, block 7,888,670, transaction `0x677c18d3363213d0bd1897cf899dca5b3e7be8067dfb94489b87e6764fe9e4ce`. Its persisted coordinator implementation address is still zero. None of the four has a finalized proxy initialization or ordinary/corrective upgrade activation. The complete authenticated input contains 125 plans, 59,979 journal rows, and two nonzero proxy identities.

Production now derives the proxy census through one pure helper shared by timeline reconstruction, coordinator receipt selection, release capture, and baseline capture before dialing RPC. Only an explicitly approved legacy v1-v3 predecessor with an empty decoded deployment, no upgrade identity/baseline, no coordinator repair carry, and no retired deployment can contribute no proxy. Missing or foreign plans, a zero active proxy, modern empty deployments, partial deployment identities, and finalized transitions under a zero-proxy plan are rejected. The three exact transition IDs are shared with the timeline classifier: proxy construction, normal activation, and corrective activation. Finalization refusal precedes kind, deployment, and height classification, including zero and campaign/postcampaign heights. Static planned actions and authenticated native/implementation preparation remain valid. Preparatory Evm execution still lowers the release capture floor; native block numbers do not.

The adjacent audit found that the release emitter helper already understood all-zero placeholders, while timeline reconstruction did not. The receipt selector could omit an unbound transition because its symbolic or zero target was not a live proxy; baseline capture could reach its dial before detecting that inconsistency. Those paths now use the same census guard. Independent timeline artifact replay already invokes the timeline builder and inherits the correction. Current deployment identity, per-receipt executable identity, baseline address validation, real-proxy initialization, observed upgrade ownership, and archive range verification retain their existing strict checks. No additional emitter, transaction authority, or transition action is admitted.

`pre-fix.red.log` records the deterministic refusal from base `1b4751be15a79a5dc905b854f2a5dcc337767792` after an actual historical decoder authenticates the small synthetic v1 plan. `pre-fix.fixture.go` preserves the exact regression fixture. New positive controls cover unchanged timeline/artifact reconstruction, future symbolic actions, finalized native and implementation preparation, and live baseline selection through a synthetic endpoint. The 32 mutation controls cover ancestry, source identity, deployment shape, old/new schemas, preparatory intent, and all three forbidden finalized transitions at four boundary heights, with adjacent alias and kind/deployment disguises. A dedicated baseline control proves an unbound transition is refused with zero synthetic RPC calls.

All 58 selected top-level tests passed normally in 25.112 seconds. The first broader normal run exposed an incomplete pre-existing target-selector fixture whose active proxy was unset; the fixture now names its already-declared proxy. The production guard was preserved. `adjacent.fixture-failure.log` retains that run. `selected-tests.json` lists the exact normal/race selection, including the previously qualified signed precompile and coordinator repairs, relay chronology, oracle artifacts, fleet sources, and baseline reorg controls. The same 58 tests passed under race in 175.925 seconds, with all 32 new mutation controls passing and no race warnings. Exact exits and source identities are recorded in `qualification.json`.

The retained compatibility overlay authenticates the original diagnostic, all selected bundle locators and bytes, the full approved lineage, the journal, original relay requests, and signed carried repairs before running the production proxy census and full release contract census. It reads the existing immutable copied inputs and writes only to this new evidence directory. `retained-replay-source.json` pins the production files used during that replay. No production source file was changed after replay start. The completed replay proves census compatibility only; the parent task owns captured chain chronology and final diagnostic qualification. There are no live RPC calls, live-state writes, sealed-v3 writes, or validator recaptures in this work.

Qualified commit: `1ee38f72e4acd776937eaf8f2fe6d4e66bef1e3a`; tree: `1bc4689d42413f097294f9b0bf8e893b5c0c5c43`. The isolated worktree is clean. The complete retained replay passed in 475.944 seconds and produced the same ten-emitter census from block 7,888,670, byte-for-byte: SHA256 `c62fa579d94234b0bf7a8e8cb7569af611337c031770328461c3122b24cb489f`. Its exact receipt SHA256 is `76e9e53e2348ab9a8e1036e881b2d976972adae575ff1f54fefa766c96940d1c`. The correction is ready for integration.
