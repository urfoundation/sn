Commit `e1fe2daf4841729599dda77793e215b46fc84899` on `codex/r46-exit-gap-root-20260926` fixes a proven adjacent contract-prefetch defect. It does not establish the cause of R46's 18 exit-gap events or waive its 21 process-health findings. R46 remains failed, with its sealed evidence unchanged.

The sender could emit a successor announcement as its first outstanding packet after the receiver had retired during idle. When that announcement was promoted to a head, it carried the successor's proof but did not establish the current contract. The receiver repeatedly rejected it with `No contract`. The fix defers an announcement while no acknowledged packet is outstanding, so the current-contract head recovers first. Sparse or NoAck-only traffic can consequently defer an announcement until an acknowledged packet is outstanding; no contract, deadline, budget or acceptance threshold changes.

Seven new deterministic tests use production send/receive workers, protobuf wire codecs, standard SHA256 HMAC grants, generated identities, actual encrypted sessions where applicable, in-memory routes, fake time and joined lifecycle barriers. They cover plain/encrypted idle recovery, combined receive/cipher loss, single feedback loss, permanent feedback loss retaining strict failure, and plain/encrypted threshold crossing. The threshold tests also verify exactly one normal successor announcement and acknowledgement behind the recovering head, with no duplicate payload.

Qualification: 56 focused tests pass normally and under `-race`, with zero race reports. Both threshold regressions fail against the exact deployed pre-fix production source via an overlay. Adjacent controls include wrong-contract requests, foreign/malformed handshake generations, contract HMAC cutover, malformed contracts, cancellation and pool ownership. The first race run identified unsynchronized reads in the test fixture; final qualification replaces those reads with locked snapshots, atomic observations and worker completion barriers. Exact argv, test names, source hashes and logs are in `review.receipt.json`.

All 18 sealed receive gap witnesses pair with same-peer sender lifetime exits. Each sender wrote nine times, and its first unacknowledged number is one below the queued receiver tail. Prior ACK to next write spans 154.305–295.318 seconds, between the 120-second receive idle and 300-second send idle bounds. That timing is insufficient by itself: the signed and encrypted idle controls recover. Explicitly dropping every missing-contract response reproduces a sender timeout followed by a receiver gap, but R46 did not retain per-message request/write/decode evidence to prove that loss occurred there. The exact peer-window scan finds zero `No contract` or contract-verification errors across all 18 events, so the adjacent prefetch fix is not their attributed cause.

The 20 swarm health findings were observed during runtime at 18:10:43, from supervisor state at 18:10:16, with live unchanged PIDs and restart counts. The Substrate RPC proxy finding was observed at 18:15:44 from state at 18:15:18. These precede teardown. Their boolean-only saved health cannot distinguish a probe timeout, non-2xx readiness response, registration loss or carrier loss. Later independent captures show 19/20 swarms healthy at 19:06 and 20/20 at 20:47; they do not erase the earlier findings. The two validator restart anomaly rows likewise reflect runtime state changes and remain in the validator readiness investigation.

The next diagnostic reproduction needs bounded per-sequence head/proof/role/route observations, missing-contract request and ACK write/decode dispositions, and failed health probes with deadline, timing, transport-error class or HTTP status and bounded sanitized response. Existing `acklineagetrace` hooks require an installed bounded observer; the build tag alone records nothing. Keep all transport and process-health verdicts strict, and do not expand expected fault targets without a causal witness.
