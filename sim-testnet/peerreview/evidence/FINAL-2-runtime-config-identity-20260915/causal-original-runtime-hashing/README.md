# Original-runtime-hashing causal

This captures one disposable, normal-mode production mutant. The sole mutation
removes `ExpectedRuntimeSpec` substitution while retaining optional-pin
clearing. The qualified candidate and all dependency aliases remain clean.

The first setup refusal is retained because it stopped before worktree or
compiler admission. The corrected `r2` owner compiled once and ran exactly five
roots, producing the expected three failures and two passes. `MANIFEST.tsv`
references retained raw evidence; it does not duplicate source, caches or binary.
