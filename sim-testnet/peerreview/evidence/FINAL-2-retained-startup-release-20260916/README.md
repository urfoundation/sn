This bundle records the completed **read-only release-lock render** for frozen source `e109ac35c5ea5ff5006040c2987118e99627e863`. Tool session 84281 joined normally; raw body, wrapper and join exits are all 0. Body timestamps: `2026-09-16T11:09:05Z` to `2026-09-16T11:09:07Z`. State, executable, installed lock and source-revision fences are byte-identical before and after. The renderer's relevant-input comparison against the qualified aeda executable source is empty.

The exact lock diff changes only `repositories.sn_go_source_hash`, from `sha256:2ac35d3ed59965e06ba68c5c756139c7b77c43bc11d0c5ec3ba93dc5f0823c20` to `sha256:b4d79718f2e227882fd965d048f3a0cb71ffcac1e88deb82d7d2c0ac0062d96a`. All other lock lines are identical. Original lock SHA256: `bf417189d4c0a62f8116606f84f9c5509b3afe2dab611429c9b781998b3198fc`. Emitted candidate SHA256: `42a48da4d4fd267c496b8558cd9838f68b3f5e4117a9e681352129069df2df22`.

The qualified existing executable rendered current clean repository observations without `--apply`. Native complete before/after repository checks, schema validation and static-field preservation remained active. The exact command/request, stdout/stderr, original lock, diff, root review, raw exits/times and fences are retained under `renderer/`. `COPY-INDEX.json` binds every copied receipt to its original capture path, size and digest. Original absolute paths inside raw command/hash receipts are preserved; use this bundle's relative-path manifest for portable integrity checking.

At capture close the candidate lock was uninstalled and no chain writes occurred. This receipt covers rendering only. It makes no claim about subsequent qualification, final executable build, publication, setup adoption or native recovery. Executable bytes, private config/vault contents, service data and live deployment file contents are omitted; only their applicable hash receipts are retained.

Verify the sealed payloads with `sha256sum -c SHA256SUMS` from this directory, and verify the manifest with `sha256sum -c SEAL.sha256`. All copied bytes were compared with the closed originals during packaging. Packaging performed no renderer invocation, project test/build, source edit, commit or publication.
