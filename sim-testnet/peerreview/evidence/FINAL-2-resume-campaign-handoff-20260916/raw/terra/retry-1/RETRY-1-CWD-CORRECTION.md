The original three compiler invocations are preserved at the parent capture with build/outer/join exit 1 and no body start. They invoked `go test -c` while the launcher current directory was its capture directory, so Go reported no go.mod. This retry changes only the local launcher working directory: each compiler explicitly `cd`s to its already-admitted absolute selected source before the same command, selector, source/dependency fence, cache, and absolute output path are used. No source, selector, dependency, or test body changed.
