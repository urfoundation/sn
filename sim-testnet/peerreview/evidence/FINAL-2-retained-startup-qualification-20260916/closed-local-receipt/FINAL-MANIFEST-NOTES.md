`CLOSED-MANIFEST.tsv` remains the valid initial receipt closure. The raw
capture index was added afterward to bind all closed raw files by path and
hash, so this FINAL manifest supersedes it as the complete local receipt seal.
No test, source, binary, or raw capture file changed after the initial closure.
