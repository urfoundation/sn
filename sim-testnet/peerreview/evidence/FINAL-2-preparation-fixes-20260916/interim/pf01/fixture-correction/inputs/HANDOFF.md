# Frozen PF-01 cold fixture correction

Clean isolated source `/home/by/urnetwork/temp/sn-carried-preparation-fixture-20260916`, commit `d52028de6864f7b1c48381fcb7652361d2520932` (tree `ae04062798abdc6f9ee35b6c6be36449f5f773a7`), parent `351ece79d9f4dad93888c74c8bdcc699dd4c8dac`. Exactly one file changes: `sim-testnet/fleet_history_batch_test.go`, SHA-256 `1520f93a7ec5429b2fd013dcc6a3ec2006980cd3c2755ec443a09e4772e332b8`. `correction.patch` SHA-256 `29cced2f72cad66d819ca7a9d7af00facf4d530756015825c7525474dd94ab83`. No production/shared-fixture/source-adapter change; primary, physical source and live state untouched. Formatter: gofmt on this Go file; git diff --check passed before commit. Implementation ran no tests or builds.

The actual 32-root normal and race bodies each retained 31 PASS and the same one FAIL: the second direct verification expected a cold 6/2/2 cumulative HTTP/header/contract count after its first call had populated the existing durable historical cache, which correctly produced 5/2/1. This direct test does not exercise the new collector cache. The correction requires that the first call populated the fixture-local durable cache, removes only that temporary directory, then retains the complete second cold 6/2/2 assertion and every collected receipt/identity/cancellation negative. That independent postcondition now reports Errorf, allowing its later controls to run. No validation was weakened.

## Exact incremental qualification

`selector.txt` selects exactly 2 top-level roots in `roots.txt`: changed `TestCarriedFleetHistoryBatchCachesOnlyExactVerifiedAction` and unchanged adjacent `TestHistoricalFleetCacheReusesAcrossExecutorsWithBatchedFreshCheckpoints`. Expected 2 PASS, body exit 0, in one uncached normal and one uncached race body. The second root deliberately verifies durable reuse across executors while refreshing canonical checkpoints; it guards the boundary isolated by this fixture edit. No shared fixture consumers changed. Reuse the other retained 30 passes per mode, and preserve the prior warm-control passes as earlier evidence. Do not rerun all 32. Preserve both actual original failed invocations.

The original causal 8-root body already yielded the intended exact 2 FAIL / 6 PASS map. No files consumed by those roots changed here; retain it without a causal rerun. Original candidate/causal source, binary and event artifacts remain immutable.

Use the original `/mnt/data/sn-testnet/qualification/carried-preparation-index-candidate-20260916-r1/manifest.json` dependency fences and Go go1.26.6. Its exact SHA-256 is `d72d923d8ac264c14b7364ab8623b9604eee1155f5084b38a82a29c60a09a9cb`. The unchanged eight new tests and all production files are byte-identical to 351ece79. Materialize this clean source into Terra's existing isolated USB workspace only after every prior owner has joined. Use the established normal/race compiler/body harness, original GOCACHE `/mnt/data/sn-testnet/gocache`, and separate private TMPDIR/GOTMPDIR below this correction's capture root. Fence source/dependencies/selector/binary before and after each owner; expected body exit 0 is a requirement, not a claimed result.

No new fixture uncertainty beyond ordinary compilation/execution remains identified: the cache directory constant is the production durable-cache location, `stateDir` is this root's t.TempDir(), and deletion is solely fixture setup before the existing second cold proof. The prior full normal/race batches found no other failures to batch.
