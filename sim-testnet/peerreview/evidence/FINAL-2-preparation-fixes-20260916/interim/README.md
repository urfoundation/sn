# Preparation fixes: closed qualification receipts

This portable bundle accepts PF-01's **32 distinct roots per normal/race mode** and preserves PF-02's **9 SN roots per mode as partial qualification**. PF-02 server qualification is still pending in this seal. These are synthetic qualification results; they do not establish successful native startup, a running soak, or release-candidate completion.

| Closed capture | Actual selected-root outcomes | Raw body / outer / join exit | Use |
| --- | --- | --- | --- |
| PF-01 original normal and race | Each 31 PASS, 1 FAIL | Each 1 / 127 / 127 | Retain 30 unaffected passes per mode; preserve the actual failed invocations. |
| PF-01 fixture confirmation normal and race | Each 2 PASS | Each 0 / 0 / 0 | Replace the changed fixture and confirm its adjacent warm-cache control. |
| PF-01 causal normal | 2 expected FAIL, 6 PASS | 1 / 1 / 1 | Exact intended old-work behavior reproduced. |
| PF-02 SN normal and race | Each 9 PASS | Each 0 / 0 / 0 | Accepted SN portion only. |
| PF-02 SN causal normal | 2 expected FAIL, 1 PASS | 1 / 1 / 1 | Exact intended unbound-operator-spec behavior reproduced. |
| PF-02 initial server environment refusals | 2 censuses; no selected test body started | Each 125 / 127 / 127 | Not admitted; not semantic test failures or passing controls. |
| PF-02 portable owner refusals | 7 censuses; no selected test body started | Each 125 / 127 / 127 | Not admitted; setup and cleanup each exited 0, but the owner attempted to wait on non-child launchers and cleaned up before their censuses. |

PF-01 implements one detached journal snapshot/index and call-local authenticated historical-plan reuse during actual carried receipt collection. It preserves complete admitted ancestry, latest eligible journal selection, every receipt's hash/identity and original RPC-label checks, current postconditions, cancellation and the existing durable audit cache. A final journal snapshot rejects an append/change before publishing verification keys. There is no cross-command source cache. Eight new deterministic top-level roots cover bounded collected work, multiple sources, journal selection, corrupt source/receipt, changed scope/authority, append and cancellation/retry; the adjacent original roots cover owned RPC, carried history, historical fleet and durable-cache boundaries.

The PF-01 production candidate is `351ece79d9f4dad93888c74c8bdcc699dd4c8dac`; its test-only correction is `d52028de6864f7b1c48381fcb7652361d2520932`. The initial direct fleet-history test warmed the durable cache, then incorrectly expected a second cold proof (actual HTTP/header/contract counts 5/2/1). The correction requires the fixture cache to exist, removes only that temporary cache before the second call, and retains the exact cold 6/2/2 assertion and receipt/identity negatives. The independent assertion now continues to adjacent checks. Production behavior is unchanged. The causal source `57983498878ba1bc9e84dcc4f5502580ccd3d5f6` restores per-action journal and source reads through the same counted boundaries: 66 snapshots / 4224 copied rows and 64 source decodes fail the bounded-work roots, while all six controls pass.

PF-02 binds the explicit subnet-operator workload to both managed operator taskworker specifications and preserves it through supervisor serialization. Its server proposal scopes admission and scheduling to required existing tasks, including ST reconciliation and verification, while retaining ordinary default behavior. The frozen SN source is `8e6d56b5a40c897a91ce80b94439239ce29de4ef`; its causal source is `e6e657900a6f14927537736d7a9912bf81992995`. Server task/taskworker bodies have no accepted result in this bundle. A later closed server qualification must be supplied as a separate sealed subbundle; none of the refusals recorded here may be reclassified.

## Raw evidence and portable verification

`pf01/` contains the complete closed original, correction and causal compiler/body receipts, admitted inputs, selectors, compiled censuses, raw test2json events, actual exits, source/dependency/binary/selector fences and rejected initial workspace admission. The initial chmod-altered workspaces were never compiled or run. `pf02/` contains admitted source/selection records, SN compiler/body receipts, all closed original server compilers and the nine server census refusals. `prepared-not-admitted/` preserves the earlier preparation-only record. No compiled binary, temporary test directory, service state, runtime configuration, environment file, credential or private native log is included.

`pf01/final-receipt/` and `pf02/sn-partial-receipt/` preserve Terra's authoritative indexes byte-for-byte. All 200 PF-01 and 120 PF-02 raw-artifact digest references resolve to exact copies through `CAPTURE-MAP.tsv`. `interim-composition/` retains the original composition receipt. Its absolute capture references and all captured command paths remain provenance; they are not commands to rerun or overwrite those captures.

`ACCEPTED-ROOTS.tsv` reconstructs exact accepted membership from copied raw top-level terminal events: 30 retained original PF-01 roots plus both confirmed roots in each mode, and nine SN roots in each mode. `INVOCATIONS.tsv` preserves all 17 closed body/census outcomes, including non-admitted attempts. `COMPOSED-ACCEPTANCE.json` records the explicitly partial composition. Packaging checked exact selectors/membership and before/after source, dependency, binary and selector equality; it did not run any compiler or test. Original PF-01 `events.valid=false` remains intact because those invocations actually failed their all-pass expectation; all corresponding source/dependency/binary fences remained valid.

`SHA256SUMS` hashes every public payload using paths relative to this directory. `SEAL.sha256` binds that manifest. Verify with `sha256sum -c SEAL.sha256` and `sha256sum -c SHA256SUMS` from this directory. Original inner manifests retain their original capture paths; the portable outer manifest is the copied-payload authority. Root controls any later integration and native recovery.
