# PF-02 SN partial qualification receipt index

This index seals the completed SN-only PF-02 lane. It contains references and digests for the 9-root positive normal/race bodies and the 3-root unbound-spec causal body. Server task/taskworker results are intentionally excluded because their bodies are pending a separately-owned disposable test-environment retry; no server test outcome is represented as a production result here.
