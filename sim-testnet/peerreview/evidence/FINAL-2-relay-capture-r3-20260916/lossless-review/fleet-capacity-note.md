The renewed fleet interval remains 393–424 for all 202 fleets and for the lifecycle takeover reference. No renewal action, signature, economic allowance or validity endpoint changed.

The continuation's settlement end 433 is a capacity forecast: 406 + ceil((8025298 − 8017249) / 300). Its native end 1510 is 1487 + ceil(8049 / 360), using the conservative configured tempo in the relay forecast. These fields reserve possible relay subjects; they do not alter any fleet binding. Source: evidence_relay_continuation_capture.go:354–373 and evidence_relay_continuation.go:142–192. The complete retained subject forecast is 766 of 1024 slots: 760 closed/future buckets plus six retained older audit subjects. The signed ledger heads and all four source identities/activations/history prefixes are unchanged. The changed filesystem metadata and two storage totals are separate observations.

Actual acceptance remains checked at fresh post-preparation snapshots. buildScenarioAcceptanceWindow uses current settlement epoch + 1 as the first complete epoch (scenario.go:439–498). fleet_lifecycle_runtime.go:1025–1075 applies validateFleetLifecycleTakeoverWindow to both phases; fleet_lifecycle_renewal.go:206–225 requires first >= 393 and first + count − 1 <= 424. Thus the exact individual window ceilings are M2 first <= 420 for five 300-block epochs, and M3 first <= 422 for three 360-block epochs. Their terminal finalized observations also require the respective +150/+180 block tails. These are necessary per-phase bounds, not a promise that the phases can begin independently at those latest epochs.

Production preparation follows the authenticated M2 result and requires a later production policy, the dishonest-deposit/audit phase, and fresh native warmup. Those actual waits can push M3's baseline later; the native arming checks must still pass. The observed capture settlement epoch 406 leaves possible lease capacity, but this read-only continuation approval does not establish the eventual M2/M3 baselines. Forecast end 433 alone proves neither lease expiry nor a need for a third renewal. Existing actual acceptance checks decide this without waived epochs or work.

The fixed relay END is 8025298; the complete 7570-block work must still fit at startup, so the latest admissible full-work start block is 8017728. Capture's final runway check passed; import/startup recheck their current boundaries. No epoch or block deadline is moved by this review.
