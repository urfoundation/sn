# R47 provisional artifact transport continuation

Implementation commit: `d2dc2d0b70d3c4e91f1e487f5739c56228c2f105`. The retained R47 run is still a failed, incomplete run; this change does not amend its process rows, observations, signed sources, or final result.

R47 stopped on its heartbeat process-log gate at block 8,091,300, before terminal 8,092,324. Its four blocking findings were operator-1 API warning count 2, operator-2 API warning count 1, and the two validator steering-attempt failure aggregates. The API records reported `sn_attempt_artifact_handlers.go` streams with `context=<nil>` and quoted TCP `read: connection reset by peer` errors. The retained R47 result SHA-256 is `1f89032d92aa3ac694288742880c11e86f021adbed2b05c4a79a626beaa9e951`.

The R46 generic heartbeat continuation boundary already examined every retained finding and every joined error. It failed to cover R47 because the API records remained generic `warning` rows. That branch authenticates a single canceled client request, which excludes both the nil context transport reset and operator-1's count of two. Validator steering failures were already eligible for provisional continuation.

The fix introduces the exact blocking `artifact-stream-transport-reset` class and permits this class for operator API roles during authorized provisional release/soak observation. It parses the handler, stream kind, full hash, nonnegative byte count and duration, nil context, quoted read/write reset, matching operation, and canonical source/destination endpoints. It accepts all valid field values rather than R47-specific hashes or addresses. Integrity errors, extra joined causes, deadlines, unrelated warnings, other roles, process-health errors, and custody/I/O errors remain stops. The final strict gate still rejects every retained reset. Classifier v13 preserves existing v12 rows and cursor custody without reclassifying history.

`red.log` records four failing regressions against the parent implementation. The real heartbeat fixture ended at block 3,200 instead of 5,050, leaving its fault active. The fixed fixture reaches 5,050, restores the fault, preserves the baseline and aggregated finding, and returns a failed final result through `process_log_completion`. Normal and race suites passed, including process-log migrations, provisional continuation, interval and hard-stop tests. Exact commands, source hashes and log hashes are in `validation.json`; test data uses reserved synthetic endpoints and no live credentials.

The transport parser currently reuses the existing cancellation header validation pattern also used by client-disconnect classification. Extracting a shared header parser is an adjacent readability cleanup; no broader transport exception is required for this fix.
