# Historical release-lock admission: positive qualification

This portable subset records only the closed positive qualification of formatter successor `3ffc1277d1acd1e21b908c3b2cfd37612602e07b`. It contains the exact 36-root normal and race streams, compiler/body terminal records, the two-root normal p2/p3 confirmation sequence, source/dependency/binary fences, and formatter identity.

The 36 selected roots have no declared descendants. Both matrices have 36 top-level PASS records and no top-level failures. Event-line counts in `RESULT.json` describe raw test2json stream lines; they are not descendant-PASS claims.

Excluded: compiled test binaries, cache or temporary directories, and the separately owned historical-call causal subtree. Original raw records remain at the paths indexed in `RAW-LOCATORS.tsv`.
