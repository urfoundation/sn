# Historical runtime455 admission causal

This compact external bundle records the deterministic production-mutant causal for
`3ffc1277`. A forward one-line patch in a disposable checkout restores the former
current-only release-lock validator. The exact four-root normal body produced the
required two expected failures and two passes. Candidate and dependency snapshots,
binary identity, and the mutant diff remained unchanged across the body.

The retained raw capture is the sibling parent of this bundle. `MANIFEST.tsv` maps
each copied compact record to its original relative path and SHA-256. No source
checkout, binary, module cache, dependency checkout, or live process is copied.

Two setup-only refusals preceded the real compiler. Both state that no compiler
started; the r2 compiler/body is the sole causal execution. A subsequent seal-only
shell operand refusal is also retained and did not replay any qualification.
