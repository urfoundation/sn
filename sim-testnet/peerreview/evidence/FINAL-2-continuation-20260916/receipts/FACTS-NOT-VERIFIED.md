This bundle does not independently reproduce LAN RPC observations: `independent_rpc=false`. It records the native commands' captured local observations only.

It does not establish final campaign acceptance, completed startup, managed resume completion, or the root cause of the original HTTP stall. The original failure remains available through the committed provenance bundle referenced in the README.
