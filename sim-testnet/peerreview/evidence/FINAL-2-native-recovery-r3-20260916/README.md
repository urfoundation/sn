# Native r3 setup preparation receipt

The closed setup preparation ran **2026-09-16 08:12:47–08:27:00 UTC**. Body and outer owner exited 0; root joined session 49000 with exit 0. The raw captured exits/timestamps are retained in `setup/`. The result reports `ready=true`, `prepare_only=true` and `stopped_before_actions=true` for plan `0x49ddbc495a51c7c089ed5838299d6d65fb40cfb9b35be3ee38cd4ca1de7aa876`.

All **4673 carried audits** completed, and all **nine hard preparation checks** passed. The nested doctor result is ready, with 61 hard checks passing and two non-hard checks reporting false. `launch-runtime-inputs` is a non-hard deferred check: config.render was already verified, and future namespace/adoption authority was deferred to launch/resume. It is preserved as `ok=false, hard=false`, not rewritten as a pass. `setup/RESULT-SAFE.json` records every check's original name and boolean result and the exact deferral text.

Captured executable and release-lock hashes are unchanged. Source revision is `aeda6abbd2dc0abc92bb0f60975cf89b509e8017`; CLI SHA-256 is `8fc61a65cd0524413a7ba70c61bcdb15962fa87ad7ab347b653abb27f8913f0b`; release.lock file SHA-256 is `bf417189d4c0a62f8116606f84f9c5509b3afe2dab611429c9b781998b3198fc`. The raw request retains the exact approval scope, LAN authority, preserved actions/history and the prohibition on additional spending or duplicate actions.

Among the six captured state files, only `plan.json` and `config.redacted.yml` changed. Journal, identities and both supervisor files are unchanged. The saved plan file SHA-256 is `438ecc8621a26c945180d5027c8bbfb734a82733d4f30c84017b4e122a3c8bd9`; the result binds its approved plan identity above. `state_compare_exit=1` is preserved because adopting plan/config changes was expected; it does not make the setup command fail. `setup/STATE-CHANGES.json` identifies exact before/after hashes. Journal stability alone is not used to infer a blanket absence of independent background transactions.

The original stdout is 20925 bytes, SHA-256 `44cb581fa556f1cff63bb3e60118e255da8a17669a32a85fff944c16380f5e9b`, at `/mnt/data/sn-testnet/qualification/native-recovery-20260916-r3/setup-revision/stdout.json`. Its structure and result fields were inspected before projection. Full doctor/runtime detail strings are omitted; only an explicit safe field projection is published. Original stdout/stderr identities are in `OMITTED-ARTIFACTS.json`, and `setup/audit-progress.txt` contains exact allowlisted progress lines. No runtime config, secret/private value, executable, service state or private log is copied.

This bundle covers **setup preparation only**. The continuation capture running separately is not represented as completed. No continuation import, history adoption, strict startup, soak or release-candidate result is claimed. The prior sealed build/plan bundle `FINAL-2-preparation-adoption-20260916` is unchanged. Packaging ran no tests, builds, source/physical mutation, commit or push; root owns later closed-result extensions.

`CAPTURE-MAP.tsv` binds the raw copied receipts. `SHA256SUMS` covers the entire public payload using relative paths; `SEAL.sha256` binds that manifest. Verify both with `sha256sum -c` from this directory. Original command paths are provenance and must not be replayed into a closed capture.
