# Failed startup transaction reconciliation

The 2026-09-16 strict startup (owner 97810; launched generation at 06:28:13 UTC; failed log gate at 06:28:53 and exited at 06:29:05) **did submit four operator transactions**. All four succeeded in canonical blocks below the observed finalized boundary. Deployment-journal stability is not evidence of an absence of background transactions. This handoff preserves those writes and the original database statuses; it does not claim the production reconciler has already updated them.

`operator-intents.sql` was run read-only against both retained operator databases at 06:58 UTC. It selects the exact testnet deployment, all intents/attempts created or updated since startup, nonterminal intents, and failed/canceled signed attempts; it includes every attempt for each selected intent. It omits signed bytes, credentials, calldata and error text. The four new execution attempts each have generation 0 and attempt 1. The raw projected records retain their complete intent IDs and logical keys in `operator-{1,2}.jsonl`.

| Transaction hash | Signer / nonce | Decoded call | Inclusion block | Original DB intent/attempt status |
| --- | --- | --- | --- | --- |
| `0xa62a69f38c314fce4efdc6cf9084ae5a2efa7178faca0ba25b0c63bd8e5368d9` | operator 1 / 106 | `finalizeOperatorEpoch(316,1)` | 8016489 | mined / mined |
| `0x50cc7f2745b22052f91a3ce124f18fcbbe0fb0d297f59a9f3f9d2bd33e702dd8` | operator 2 / 104 | `finalizeOperatorEpoch(316,2)` | 8016488 | finalized / finalized |
| `0x62a2464cf084c69c81040d1374abcc20c8a99bb34e4788bd6e8b34cbe804d1df` | operator 2 / 105 | `deferMissedEmission(402,2)` | 8016491 | broadcast / broadcast |
| `0xe95f3f78dad211d12ced0583a47d9af610b9699d68ab08f73b2fc5c3b7acabd8` | operator 2 / 106 | `deferMissedEmission(317,2)` | 8016491 | broadcast / broadcast |

Every receipt has `status=1`; every transaction has value zero and targets coordinator `0x8e7d2f9a77fec95c7e4875b0bd858d5de2b6def8`. Operator 1's signer is `0xd6f9165c4cc35ce380ea1eff3c518407173af60a`; operator 2's is `0x5c278cf6693ce582adea0241034d810d8933b3eb`. The first two receipts each consume 105299 gas and the last two each consume 99930, all at 20134283587 wei/gas. Total actual fee: **8264277772552846 wei = 0.008264277772552846 EVM TAO**. This is incurred cost, not a new spending allowance.

The finalize selector is `0x2bfea3f9`. Each finalize receipt contains vault `RootMissed(epoch,noId,0)` and coordinator `OperatorEpochFinalized(epoch,noId,false)`. The vault changes that epoch's entitlement from Funded to RootMissed; zero funded amount is added to carry, and no payout root is created. The defer selector is `0x47068f2d`. Each defer receipt contains vault `EmissionDeferred(epoch,noId)`: the previously Unset entitlement becomes Funded with zero funding for that missed boundary. The contract leaves stake on the pool hotkey for a later timely boundary; these calls do not capture a multi-epoch stake delta. The vault address is `0x09d5d7a5c3e94b6ae42b09889a1cee50f970fc5e`; every decoded log has `removed=false`.

These decodes use the frozen SN source at `0fd7ffc0f6aaad5a2988427c4acb47e1f79c3819`: [coordinator defer](/home/by/urnetwork/temp/sn-carried-preparation-index-20260916/evm/src/STCoordinator.sol:602), [coordinator finalize](/home/by/urnetwork/temp/sn-carried-preparation-index-20260916/evm/src/STCoordinator.sol:635), [vault defer](/home/by/urnetwork/temp/sn-carried-preparation-index-20260916/evm/src/STSettlementVault.sol:312), and [vault missed root](/home/by/urnetwork/temp/sn-carried-preparation-index-20260916/evm/src/STSettlementVault.sol:349). Those source files were verified byte-identical to that commit. Local ABI signature decoding made no network request.

## Canonical and finalized evidence

Root's LAN-only `rpc-receipts` capture finished 06:59:27 UTC; `rpc-canonical` finished 07:00:26. Requests/responses, exit status and timestamps are retained here. Chain ID is 945; native genesis is `0x8f9cf856bf558a14440e75569c9e58594757048d7b3a84b5d25f6bd978263105`; runtime versions are 460/1/1. Inclusion hashes independently read by block number match each receipt:

- 8016488: `0xcbf60b807b6c2dd25bd014a013b291a763519b2ad7d21de7d0595ed639b51158`.
- 8016489: `0x8d589b6957adf23ffb84d465b118c50d4b057d74b7e67c790884eb373a3a2f48`.
- 8016491: `0x4d1936206c41d92cfc9ac9506308e502e502d1553aa683d92e8e3b3832e484f1`.

The native finalized head is block 8016641, hash `0x7ee9498db1d26737986ceca77279f9de6a1a41c6780556e6a7d75421b64663fb`. The EVM hash at that height is separately `0xa2657bf34431dbd71f5c1a75b3dbf93e513e3581c5b426cc615db8c66289d635`. Both root signers have nonce 107 at that finalized block and at pending. No additional RPC query is needed to settle inclusion/finality for these four captured attempts.

## Other startup actors and limits of the comparison

The sealed original generation evidence remains at `/mnt/data/sn-testnet/qualification/native-recovery-20260916-r2/strict-resume-failure-evidence`; raw private logs are not copied into this handoff. The pre-start history bundle SHA-256 is `e556044d5cf4b584856df3dc7c0a199a582ce14130f5818c09190ac54673eb53` in the deployment's corresponding `history-adoptions/.../bundle.json`.

Validator 1 still has no steering-intent file. Validator 2's retained 23486-byte file exactly matches the bundle prefix, SHA-256 `0de9867f8c0b699954e9ef25c66759b0390214ddaf8313d9ca7f42df63e27727`; its only three intents remain applied for epochs 1401, 1404 and 1405, last updated September 12. No new coordinator-state-v2 file changed; LevelDB startup/compaction metadata and regenerated credential-file metadata changed. The native submit path durably records an intent before sending ([release_submit_v2.go:476](/home/by/urnetwork/temp/sn-carried-preparation-index-20260916/validator/release_submit_v2.go:476), send at 516), supporting no new native steering submission in this generation.

All 1000 claim queues were rewritten. Of 334784 total entries, 51874 were updated during the active claim interval: 46784 newly discovered pending entries and 5090 retries. Every entry updated in that interval has zero attempts and no transaction hash/raw signed bytes/finality. The only four transaction hashes anywhere in these queues are old finalized September 11 entries. No submitting or uncertain entries remain. The claim daemon persists an incremented attempt before submission and persists the prepared hash/bytes before send ([claim_daemon.go:961](/home/by/urnetwork/temp/sn-carried-preparation-index-20260916/miner/claim_daemon.go:961), [prepared hook:746](/home/by/urnetwork/temp/sn-carried-preparation-index-20260916/miner/claim_daemon.go:746)). This supports discovery/retry activity without a new claim submission. No complete pre-start claim-queue hash baseline was captured, so this is a durable-record/source-order reconciliation rather than an exact queue byte comparison. The managed release/soak evidence-relay actor was not launched.

The operator ST worker has its own database-backed signer and acts independently of those queues and the deployment journal. Its four actual writes above remain part of the recovered chain history. Resume must preserve the retained database, intent IDs, nonces and signed attempts and let the existing production reconciler authenticate receipts/canonicality and persist final status. At server commit `93bc8c592797da0335f4201c5027a2b52906790e`, `controller/st_controller.go:1080` (`observeTransactionAttempts`) performs those checks and `:1170` marks a successful execution finalized. `runTransactionIntent` at `:1220` reuses durable attempts; it may rebroadcast the same signed bytes while learning a prior result. Do not manually alter DB status, allocate replacement intents/nonces or resend these operations as new transactions. This handoff introduces no new recovery command or gate.
