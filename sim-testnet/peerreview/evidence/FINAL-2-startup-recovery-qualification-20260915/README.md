# Startup recovery qualification

The effective source is `53b0e9502eca07dcfc9d0e5bf3d84a031e65df05`.
`COMPOSITION-INDEX.md` records the retained and replacement source scopes.
The exact union contains154 passing roots per mode:21 CRV4,13 miner,45 validator
and75 simulator, normal and race. `membership/` contains those exact names;
root independently compared each union with the admitted154-root selection.

`r1/`, `r2/` and `final-miner/` contain unchanged raw test events, actual exits,
source/binary/selector hashes and terminal receipts used by the composition.
The original r1 validator processes failed two diagnostic-file fixture controls;
the original r2 miner processes failed the stale source-commit expectation.
Their nonzero exits remain visible. The later exact2 and exact1 passes replace
only those affected scopes. Earlier miner compiler refusals and operational
launcher failures remain in the full capture paths named by the index.

`causal/` retains six test processes whose actual exits are1 because they
reproduce exactly12 expected failures and6 passing controls after restoring
specified pre-fix production files. Their qualification exits are0 after checking
those expected event sets; they are not represented as passing test invocations.
The source and binary fences remained true. Large compiled binaries are retained
at the original capture paths; their hashes accompany each body.

All source changes after the combined23f391d batch are confined to three test
files: shared miner artifact return type, explicit diagnostic-file inputs, and
the independently attested459 source-commit expectation. Unaffected previously
accepted producer/aggregate coverage remains covered by its original receipts.
This bundle does not assert a new full-gate execution or live campaign acceptance.
It is local test evidence; the separate runtime459 bundle carries the owned-node
and upstream-artifact provenance. No new test or RPC was run during packaging.
